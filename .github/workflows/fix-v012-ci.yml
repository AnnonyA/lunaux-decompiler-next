name: Fix v0.12 CI

on:
  pull_request:
    branches:
      - main

permissions:
  contents: write

jobs:
  fix:
    if: github.event.pull_request.head.repo.full_name == github.repository
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feature/structured-phi-conditions-tables-v012
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Apply CI corrections
        run: python scripts/fix_v012_ci.py
      - name: Commit corrections
        run: |
          if git diff --quiet; then
            echo "No generated changes"
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/lunaux/backends/structuring.py src/lunaux/backends/lifter.py src/lunaux/backends/reconstructed.py
          git commit -m "Fix v0.12 style and conservative edge cases"
          git push origin HEAD:feature/structured-phi-conditions-tables-v012
