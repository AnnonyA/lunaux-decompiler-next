name: Fix AST mypy v0.9

on:
  push:
    branches:
      - feature/ast-precedence-v09
    paths:
      - "scripts/fix_ast_mypy_v09.py"

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feature/ast-precedence-v09
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Apply typing fix
        run: python scripts/fix_ast_mypy_v09.py
      - name: Commit generated source
        run: |
          if git diff --quiet; then
            echo "No generated changes"
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/lunaux/backends/lifter.py
          git commit -m "Fix AST expression typing in the lifter"
          git push origin HEAD:feature/ast-precedence-v09
