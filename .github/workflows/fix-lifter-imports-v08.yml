name: Fix LunaUX 0.8 lifter imports

on:
  push:
    branches:
      - feature/ssa-expression-v08
    paths:
      - "scripts/fix_lifter_imports_v08.py"
      - ".github/workflows/fix-lifter-imports-v08.yml"

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feature/ssa-expression-v08
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Fix imports
        run: |
          if [ -f scripts/fix_lifter_imports_v08.py ]; then
            python scripts/fix_lifter_imports_v08.py
          fi
      - name: Commit cleanup
        run: |
          if git diff --quiet; then
            echo "No generated changes"
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "Sort SSA lifter imports"
          git push origin HEAD:feature/ssa-expression-v08
